//creating a function
//addition
function addition(x, y) {
    var result = x + y;
    document.write(result + "<br>");
}
//subtraction
function addition(x, y) {
    var result = x - y;
    document.write(result + "<br>");
}
addition(50, 20);
addition(500, 200);